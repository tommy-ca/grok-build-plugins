## Context

Tip floor `f4dc2aee4683837dbe841bb6e7ca30575d67104a` (`f4dc2ae`) — herdr-agy-workflow-bind CLOSED (#24+#25+#26). Repo-root `EXTERNAL-LOOP.md` already holds Symphony WORKFLOW.md-shaped twin content (frontmatter-equivalent + body + Session arms + isolation trinity). Filename still EXTERNAL-LOOP while upstream Symphony and operator intent use **WORKFLOW.md**. Living lhs HARNESS/README + verify assert + living OpenSpec caps still mandate the old filename. Act-on refresh (Todd room / BRIEF): after rename, **orchestrators MUST load tip WORKFLOW.md** before dispatch (Drove refuse/hold; Horizon briefs cite; Herd follows named session arms). Box sand-workflow skill flip = Drove Apply, not gbp propose product.

## Goals / Non-Goals

**Goals:**

- NEW capability `workflow-md-rename` with ADDED requirements for (1) hard-rename, (2) living product cite flip, (3) orch-load-before-dispatch, (4) propose-only honesty.
- MODIFIED (+ RENAMED where titles embed old name) living caps that mandate the live filename: `herdr-agy-workflow-bind` (required), `gbp-external-loop-docs`, `lhs-swarm-economics-refresh`.
- Wave-4 propose-only; validate `--strict` PASS; OpenSpec PR only.
- Wave-5 Apply HOLD: rename + cites + OpenSpec merge; Drove Band B skill flip out-of-repo.

**Non-Goals:**

- Remint closed thermos / lhs / herdr-agy archives
- Rewrite WORKFLOW twin body (rename + cite only)
- Product rename / HARNESS / README / verify edit in propose PR
- Elixir Symphony daemon / invent LIVE / dual orch
- gbp git PR landing sand-workflow skill body edits
- Ad-hoc Herd path ignoring tip WORKFLOW.md

## Decisions

1. **Change id `workflow-md-rename`** — locked Act-on; hard-rename + cite flip + orch-load contract.
2. **Hard-rename prefer** — `git mv EXTERNAL-LOOP.md WORKFLOW.md`; no thin redirect stub unless a later ADR requires (BRIEF prefer hard rename).
3. **Content preserve** — Session arms Herd→herdr→agy + isolation trinity (CAO cloud VM / Herd herdr pane+kind=agy / Heavilifter on-box worktree) stay as landed by herdr-agy; this change does not remint twin prose.
4. **Living cite surfaces (gbp git Apply):** `long-horizon-swarm/HARNESS.md`, `long-horizon-swarm/README.md`, `scripts/verify-long-horizon-swarm.sh` assert string.
5. **OpenSpec living-filename honesty:** RENAMED + MODIFIED full requirement blocks for caps that mandate the live path; copy entire blocks before edit.
6. **Orchestrator load contract (NEW):** Drove continuous tick loads tip WORKFLOW.md before spawn and refuse/holds if missing/incomplete keys; Horizon leaf briefs cite tip WORKFLOW.md for Session arms / isolation trinity / lever-first VERIFY; Herd follows herdr→agy as named in tip WORKFLOW.md.
7. **Split apply ownership:** gbp git Apply (Horizon/Heavilifter) = rename + lhs cites + verify assert + OpenSpec merge; Drove/Opus/eggbot Band B = sand-workflow skills (`drove-external-loop`, `fleet-org-raci`, `inner-outer-orch`, `eng-lead-merge-authority`) say "read tip WORKFLOW.md" + persona/roles-map cite flip — **tasks Band B only**, not propose product files.
8. **No new durable ADR** — rename + orch-load follows Symphony upstream + existing fleet skills; cite catalog ADRs 0001–0006; no Elixir daemon ADR.
9. **Propose-only Wave-4** — product rename lands Wave-5 after Todd/Horizon go.
10. **Tip floor locked:** `f4dc2ae`.

## Risks / Trade-offs

- [Orch continues using EXTERNAL-LOOP path after rename] → Spec + tasks: orch-load MUST; verify assert flips; Drove Band B skill flip named.
- [Drove spawn without WORKFLOW.md] → Spec SHALL: refuse/hold when missing or missing required keys.
- [Confusion with reminting herdr-agy] → Explicit NEW capability; cite closed archive; rename-only content preserve.
- [Box skill flip claimed done from OpenSpec prose] → Tasks Band B Metadata; propose MUST NOT claim skills reminted.
- [Thin redirect stub creep] → Prefer hard rename; stub only if later ADR.
- [Isolation trinity / session arms regress on rename] → Prove bars require preserve; no body rewrite in apply.

## Migration Plan

1. Propose artefacts on worktree `docs/openspec-workflow-md-rename` from tip `f4dc2ae`.
2. `openspec validate workflow-md-rename --type change --strict` → PASS.
3. OpenSpec-only PR; do not merge as apply; do not rename product file in propose PR.
4. Wave-5 after Todd/Horizon go: hard-rename; flip HARNESS/README/verify cites; merge capability deltas; re-validate; prove; archive.
5. Drove Band B (parallel / after gbp apply): flip sand-workflow skills + persona/roles-map cites to WORKFLOW.md.
6. No tag move. No Elixir daemon. No LIVE invent. No remint closed Act-ons.

## Open Questions

None that block propose. Wave-5 may choose whether any historical EXTERNAL-LOOP string remains in archive/changelog prose (allowed) so long as live path and living asserts are WORKFLOW.md. Exact refuse vs hold UX for Drove incomplete WORKFLOW.md is author choice within SHALL refuse-or-hold.
