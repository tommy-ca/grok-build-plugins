## Context

Tip floor `4c126d8e3b6413fe0e868c3e3c88f8e99a7e15c0` (`4c126d8e`) — lhs archive #23 MERGED; EXTERNAL-LOOP.md already live from archived `gbp-external-loop-docs` as a **thin pointer**. Horizon Act-on REFRESH + BRIEF: stop thin-only cite; **rewrite** EXTERNAL-LOOP as Symphony WORKFLOW.md-shaped twin (frontmatter-equivalent runtime + Markdown body contract) including Herd→herdr→agy session arms. Upstream: https://openai-symphony.mintlify.app/setup/workflow-file + research audit. Drove B1 Session arms VERIFIED on box SoT. Personas B1 VERIFIED. Closed thermos / gbp-external-loop-docs / lhs cite-only.

## Goals / Non-Goals

**Goals:**

- NEW capability `herdr-agy-workflow-bind` with ADDED requirements for WORKFLOW-shaped EXTERNAL-LOOP rewrite + session arms + box SoT honesty + optional HARNESS cross-cite.
- Wave-4 propose-only; validate `--strict` PASS; OpenSpec PR only.
- Wave-5 Apply HOLD: rewrite EXTERNAL-LOOP.md; optional HARNESS one-liner; merge capability; prove WORKFLOW shape + herdr→agy; verify scripts green if touched.

**Non-Goals:**

- Thin-only cite patch omitting frontmatter-equivalent + body contract
- Remint closed archives or personas B1 / Drove B1 skill bodies
- Vendor sand-workflow skill bodies / clone Elixir Symphony / Liquid Codex prompt as product SoT
- Invent LIVE / LIVE_PASS
- Dual orch
- Clone arena into HARNESS
- Product EXTERNAL-LOOP.md / HARNESS edit in the propose PR
- MODIFY living `gbp-external-loop-docs` (cite only)

## Decisions

1. **Change id `herdr-agy-workflow-bind`** — locked Act-on; WORKFLOW twin rewrite + session path bind.
2. **NEW capability only** — no MODIFY of living tip specs; cite `gbp-external-loop-docs` / lhs / thermos as closed.
3. **Rewrite to WORKFLOW.md shape (map, not clone)** — EXTERNAL-LOOP becomes gbp's versioned WORKFLOW twin:
   - **Frontmatter-equivalent runtime** (YAML fenced block preferred, or equivalent keyed table): `tracker`/`board` → boards / `long-horizon/*` + OpenSpec tasks; `polling`/`tick` → **Drove** continuous tick when brief has goal+quota+TaskTree (Horizon = single-change apply/merge only); `workspace`/`isolation` → **isolation trinity (distinct, do not collapse):** CAO = cloud VM; Herd session = herdr pane + `kind=agy`; Heavilifter = on-box worktree recovery; `agent.max_concurrent` default **5** + CPU/mem backpressure (**Drove**-owned concurrency policy); `hooks` optional; `observability` → approvals journal + herd-journal.
   - **Markdown body contract:** eligibility, dispatch, reconcile, retry/stall, handoff (`VERIFIED` | `human_review` | quota), lever-first VERIFY.
   - **Session arms:** Herd → herdr → agy (herdr default; bare `agy --print` exception-only + fallback journal via herd-failure-journal); CAO = scale on cloud VM; Heavilifter = on-box worktree recovery; dual orch forbidden; trinity stays distinct.
4. **Pointer discipline retained** — still cite sand-workflow SoT ids; do **not** vendor full skill bodies; file may grow past one-pager but stays a twin/index, not a skill dump.
5. **Split apply ownership** — gbp git Apply lands EXTERNAL-LOOP rewrite (+ optional HARNESS cite) + OpenSpec merge (Horizon leaf); Drove/Opus/eggbot own box SoT skill Session arms (already VERIFIED B1 — like rooms-map).
6. **No new durable ADR** — session path + external-loop posture already live in fleet skills; this change cites.
7. **Propose-only Wave-4** — product rewrite lands Wave-5 after Todd/Horizon go.
8. **No Elixir daemon** unless Todd/Opus ask; no invent LIVE.
9. **Isolation trinity locked (distinct):** CAO = cloud VM; Herd session = herdr pane + `kind=agy`; Heavilifter = on-box worktree recovery. Do not collapse into one path. **Tick/concurrency = Drove** (Horizon is not the continuous tick).

## Risks / Trade-offs

- [EXTERNAL-LOOP becomes second SoT] → Spec SHALL: cite sand-workflow ids; forbid vendoring skill bodies; keep research cite.
- [Thin-only regress after REFRESH] → Spec + tasks require frontmatter-equivalent **and** body contract **and** session arms — all three.
- [Confusion with reminting gbp-external-loop-docs] → Explicit NEW capability; cite closed archive; no MODIFY living.
- [Remint Drove B1 / personas B1] → Spec + tasks: already VERIFIED; Metadata cite-only.
- [HARNESS grows into arena clone] → Optional one-liner cross-cite only.
- [Bare print normalized] → Spec SHALL: exception-only + fallback journal before bare `agy --print`.
- [Isolation trinity collapsed] → Spec + tasks lock CAO=cloud VM / Herd=herdr pane+kind=agy / Heavilifter=on-box worktree as distinct; Drove owns tick/concurrency.
- [Operators expect Liquid/Codex clone] → Map keys to fleet surfaces; no Elixir / no Codex app-server required.

## Migration Plan

1. Propose artefacts on worktree `docs/openspec-herdr-agy-workflow-bind` from tip `4c126d8e`.
2. `openspec validate herdr-agy-workflow-bind --type change --strict` → PASS.
3. OpenSpec-only PR; do not merge as apply; do not rewrite product EXTERNAL-LOOP.md in propose PR.
4. Wave-5 after Todd/Horizon go: rewrite EXTERNAL-LOOP; optional HARNESS cite; merge capability; re-validate; prove; archive.
5. No tag move. No Elixir daemon. No LIVE invent. No remint closed Act-ons / personas B1.

## Open Questions

None that block propose. Wave-5 may choose YAML fenced block vs keyed Markdown table for frontmatter-equivalent so long as all required runtime keys are present and machine-scannable. Exact prose for eligibility/dispatch sections is author choice within the SHALL list.
