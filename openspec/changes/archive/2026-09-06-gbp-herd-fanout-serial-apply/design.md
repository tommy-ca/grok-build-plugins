## Context

Tip floor `e3cb0c05`. Post-harden `pstack-herdr` sibling present. Box workflow at `/home/box/agent-data/workflows/herd-with-herdr/SKILL.md` leads tip skill (Parallel fan-out + serial_apply). Fleet B3 ranks interactive single-agent `--wait` as a top sequential feel driver. A1 SPLIT: this change writes herdr skill; `gbp-tasks-parallel-bands` writes template/LHS — P-parallel A.

## Goals / Non-Goals

**Goals:** Sync tip herd-with-herdr toward box Parallel fan-out default + `serial_apply` journal; keep arena/I1 local; honest tip-lag language.

**Non-Goals:** Claim tip already patched; remint harden; pre-write pending kinds as live; N×agy fake arena; invent Drove caps / LIVE_PASS; auto-apply; make herdr the tasks markup home (A2 dismiss); vendor openspec-pstack-orch.

## Decisions

1. **NEW capability `herd-fanout-serial-apply`.** Skill behaviour is the primary surface; marketplace overlay scenarios stay cite-only soft fuel.
2. **Box is cite fuel, not tip SoT.** Sync text into `pstack-herdr/skills/herd-with-herdr/SKILL.md`; skill-sync-pack lag is covered by this honesty, not a separate Act-on.
3. **Fan-out default; serial_apply exception.** Match box semantics; journal required when choosing one.
4. **P-parallel A with tasks bands Act-on.** Independent paths; exclusive worktrees at apply.
5. **No new durable ADR.** Runtime orch policy sync under existing herdr overlay decisions from harden.
6. **Propose-only Wave-4.** Skill body lands Wave-5 after Todd go.

## Risks / Trade-offs

- [Operators confuse implement fan-out with arena diversity] -> Spec + skill anti-pattern language; arena/I1 stay local.
- [Slot starvation forces serial without journal] -> Requirement mandates `serial_apply` + `slot starved` symptom.
- [Box drifts again after tip sync] -> Tip plugin sibling becomes gbp SoT for shipped overlay; later box↔tip sync is ops, not this propose.

## Migration Plan

Propose on worktree from tip. Validate `--strict`. Mirror orch+durable. STOP. Wave-5: patch tip SKILL.md from box cite fuel, merge capability, re-validate, archive after land. No tag move. No pending-kind invent.

## Open Questions

None that block propose. B6 pending kinds remain park.
