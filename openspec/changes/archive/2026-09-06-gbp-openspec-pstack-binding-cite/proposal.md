## Why

Fleet package `/workspace/fleet-external-agents/openspec-pstack-orch/` already composes OpenSpec intent-driven with pstack orch: propose gated (B8), parallelism after `tasks.md`, exclusive worktrees per leaf. Tip gbp SPEC/docs/LHS do **not** cite that binding (S8). Without a cite, operators rediscover compose rules or invent Drove quotas inside gbp. Arena A3 locks an optional Soft-after Act-on — cite only; no vendor package; no Drove `max_concurrent` invent. Soft-after A follows Parallel band convention language from `gbp-tasks-parallel-bands` (and herd fan-out from #2).

## What Changes

- Cite openspec-pstack-orch compose in tip SPEC/docs and/or openspec-intent-flow references: propose gated; parallel after tasks; exclusive worktrees.
- Point at fleet package path (or a short mirrored LHS summary) — **do not** vendor the whole package into gbp.
- Explicitly exclude Drove quota numbers from gbp product text.
- Wave-4 = OpenSpec artefacts only — cite/docs land Wave-5 after Todd go (Soft-after #1/#2).

## Capabilities

### New Capabilities

- `openspec-pstack-binding-cite`: Cite fleet openspec-pstack-orch compose (propose gated B8; parallel after tasks; exclusive worktrees) in SPEC/docs/LHS; no vendor; no Drove quotas; Soft-after band language from #1/#2.

### Modified Capabilities

- _(none — prefer ADDED under new capability; tip `grok-build-marketplace` and NEW parallel-bands capability remain cite-only soft fuel)_

## Impact

OpenSpec propose artefacts only in Wave-4. Wave-5 apply (after Todd go + Soft-after readiness) adds cite pointers in `SPEC.md` and/or `long-horizon-swarm/skills/openspec-intent-flow/` references to `/workspace/fleet-external-agents/openspec-pstack-orch/` (README + orch-binding spec + design). Merges NEW capability into tip `openspec/specs/openspec-pstack-binding-cite/`. Does **not** copy package trees into the repo. Does **not** invent Drove caps / LIVE_PASS. Does **not** remint harden. Does **not** delete B8 propose gate.

## Probe Evidence Record

- Evidence label: `Static` / `Metadata`
  - Query: Fleet binding package present; tip cite MISSING
  - Path: `/workspace/fleet-external-agents/openspec-pstack-orch/`; tip `SPEC.md`; tip openspec-intent-flow
  - Result summary: Package README/design/specs/orch-binding present; tip cite MISSING (S8).
  - Conclusion: Soft-after Act-on cite-only; no vendor.

- Evidence label: `Metadata`
  - Query: Arena A3 + I1 Soft-after lock
  - Path: `arena/SYNTHESIS.md`; `arena/A3.md`; `i1/I1.md`; `i1/change-ids.md`
  - Result summary: A3 Soft-after cite LOCKED as `gbp-openspec-pstack-binding-cite`; fold-into-tasks demoted; Drove caps dismiss.
  - Conclusion: Propose Soft-after id; ordered after #1/#2 convention language.
