## 1. Specs

- [x] 1.1 Change `pstack-enable-spawn-docs` deltas for enable, spawn types, owner/repo after add.
- [x] 1.2 `openspec validate pstack-enable-spawn-docs --type change --strict`.

## 2. Docs and tests

- [x] 2.1 README and SPEC.md name enable, EROFS, `--sandbox off`, `pstack:how-explorer`.
- [x] 2.2 Tests lock those strings. Still ban `grok plugin install pstack --trust`.
- [x] 2.3 Pin bump only after pstack `feat/pstack-session-apply` is on origin/main. Subsequent pins follow pstack origin/main.
